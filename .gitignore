.DS_Store
portal/venv/
__pycache__/
*.pyc
*.pyo
.env

# PDF lecture materials (large binary files — store locally only)
Kuran-Kerim Arapcasi/

# Vocab build artifacts (regenerate locally with build_vocab.py)
portal/vocab_raw/
portal/vocab_cache.json

# Lesson-index build artifacts (the .json itself IS committed — it's the
# permanent PDF-derived knowledge base. Cache and raw text stay local.)
portal/lessons_raw/
portal/lessons_index_cache.json

# User auth DB (local; never commit)
portal/users.db
portal/users.db-*
uploads/
portal/uploads/
portal/curriculum_cache.json

# Source scans for the Tashîh-i Hurûf reference books. The four cited books
# are committed under Dersler/kitaplar/ with clean ASCII names; this folder is
# the local original (incl. a 99MB duplicate scan) and stays out of git.
Books/
